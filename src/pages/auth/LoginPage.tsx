import PlaceholderPage from "../PlaceholderPage";

// Placeholder so AuthLayout is testable now. Replaced by the real login form
// (React Hook Form + Zod + authSlice) in Feature 3.
const LoginPage = () => (
  <PlaceholderPage title="Login" feature="Feature 3 · Auth" />
);

export default LoginPage;
